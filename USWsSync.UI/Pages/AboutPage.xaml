<?xml version="1.0" encoding="utf-8" ?>
<Page
    x:Class="USWsSync_UI.Pages.AboutPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="using:USWsSync_UI.Pages"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <ScrollViewer VerticalScrollBarVisibility="Auto" Padding="28,20,28,24">
        <StackPanel Spacing="20" HorizontalAlignment="Stretch">
            <!-- Encabezado -->
            <StackPanel Spacing="4">
                <TextBlock Text="Acerca del Sistema" Style="{StaticResource TitleTextBlockStyle}" Foreground="{ThemeResource AccentTextFillColorPrimaryBrush}" />
                <TextBlock Text="Información general de la arquitectura y sincronización con Dobra ERP." Style="{StaticResource BodyTextBlockStyle}" Foreground="{ThemeResource TextFillColorSecondaryBrush}" />
            </StackPanel>

            <!-- Card Información -->
            <Border Background="{ThemeResource CardBackgroundFillColorDefaultBrush}" BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}" BorderThickness="1" CornerRadius="8" Padding="20">
                <StackPanel Spacing="12">
                    <TextBlock Text="Bizor Sync - Integración Dobra ERP" Style="{StaticResource SubtitleTextBlockStyle}" />
                    <TextBlock Text="Versión 1.0 (.NET 8 Nativo)" Style="{StaticResource CaptionTextBlockStyle}" Foreground="{ThemeResource TextFillColorSecondaryBrush}" />
                    <TextBlock Text="Módulo empresarial para la descarga y subida de información transaccional y catálogos entre servidores locales de Dobra ERP y la plataforma cloud." TextWrapping="Wrap" Style="{StaticResource BodyTextBlockStyle}" />
                </StackPanel>
            </Border>
        </StackPanel>
    </ScrollViewer>
</Page>
