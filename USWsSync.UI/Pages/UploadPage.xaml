<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="USWsSync_UI.Pages.UploadPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <!-- Grid principal anclado al viewport sin ScrollViewer exterior que expanda infinito -->
    <Grid Padding="28,20,28,24" RowSpacing="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <!-- Encabezado (Fila 0) -->
        <StackPanel Grid.Row="0" Spacing="4">
            <TextBlock Text="Subida de Datos" Style="{StaticResource TitleTextBlockStyle}" Foreground="{ThemeResource AccentTextFillColorPrimaryBrush}" />
            <TextBlock Text="Sincroniza información desde la Base Local (ERP) hacia la Nube (API Pública)." Style="{StaticResource BodyTextBlockStyle}" Foreground="{ThemeResource TextFillColorSecondaryBrush}" />
        </StackPanel>

        <!-- Contenido en 2 Columnas (Fila 1) -->
        <Grid Grid.Row="1" ColumnSpacing="20">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="460" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>

            <!-- Panel Izquierdo: Parámetros y Progreso (Con ScrollViewer propio por si la ventana es muy baja) -->
            <ScrollViewer Grid.Column="0" VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled" Padding="0,0,8,0">
                <StackPanel Spacing="16">
                    <!-- Card de Parámetros de Sincronización -->
                    <Border Background="{ThemeResource CardBackgroundFillColorDefaultBrush}" BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}" BorderThickness="1" CornerRadius="8" Padding="20">
                        <StackPanel Spacing="16">
                            <TextBlock Text="Rango de Fechas" Style="{StaticResource SubtitleTextBlockStyle}" />

                            <Grid ColumnSpacing="12">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*" />
                                    <ColumnDefinition Width="*" />
                                </Grid.ColumnDefinitions>

                                <StackPanel Grid.Column="0" Spacing="6">
                                    <TextBlock Text="Fecha Inicio:" FontWeight="SemiBold" />
                                    <CalendarDatePicker x:Name="DpFechaInicio" HorizontalAlignment="Stretch" PlaceholderText="Seleccione fecha inicial" />
                                </StackPanel>

                                <StackPanel Grid.Column="1" Spacing="6">
                                    <TextBlock Text="Fecha Final:" FontWeight="SemiBold" />
                                    <CalendarDatePicker x:Name="DpFechaFinal" HorizontalAlignment="Stretch" PlaceholderText="Seleccione fecha final" />
                                </StackPanel>
                            </Grid>

                            <StackPanel Spacing="6">
                                 <TextBlock Text="Módulo a Sincronizar:" FontWeight="SemiBold" />
                                 <ComboBox x:Name="CmbModule" HorizontalAlignment="Stretch" SelectedIndex="0">
                                     <ComboBoxItem Content="🌐 Todos los Módulos" Tag="Todos" />
                                     <ComboBoxItem Content="🛒 Ventas y Facturación" Tag="Ventas" />
                                     <ComboBoxItem Content="📦 Inventarios y Artículos" Tag="Inventarios" />
                                     <ComboBoxItem Content="👥 Clientes y Personas" Tag="Personas" />
                                     <ComboBoxItem Content="🏦 Bancos y Tesorería" Tag="Bancos" />
                                     <ComboBoxItem Content="🚚 Compras y Proveedores" Tag="Compras" />
                                     <ComboBoxItem Content="📊 Contabilidad" Tag="Cuentas" />
                                     <ComboBoxItem Content="🔄 Movimientos y Transferencias" Tag="Movimientos" />
                                     <ComboBoxItem Content="⚙️ Sistema y Seguridad" Tag="Sistema" />
                                 </ComboBox>
                            </StackPanel>

                            <CheckBox x:Name="ChkUpdateWatermark" 
                                      Content="Actualizar marcas de agua del sincronizador con esta corrida" 
                                      FontSize="12" 
                                      IsChecked="False" />

                            <StackPanel Orientation="Horizontal" Spacing="8">
                                 <TextBlock Text="Última fecha de envío de información:" FontWeight="SemiBold" />
                                 <TextBlock x:Name="TxtLastDate" Text="---" Foreground="{ThemeResource AccentTextFillColorPrimaryBrush}" />
                            </StackPanel>

                            <!-- Botones de Acción -->
                            <StackPanel Spacing="10">
                                <StackPanel Orientation="Horizontal" Spacing="12">
                                    <Button x:Name="BtnStart" Click="BtnStart_Click" Style="{StaticResource AccentButtonStyle}" Padding="16,8">
                                        <StackPanel Orientation="Horizontal" Spacing="8">
                                            <FontIcon Glyph="&#xE898;" />
                                            <TextBlock Text="Comenzar Subida" />
                                        </StackPanel>
                                    </Button>
                                    <Button x:Name="BtnCancel" Click="BtnCancel_Click" IsEnabled="False" Content="Cancelar" Padding="16,8" />
                                </StackPanel>

                                <!-- Botón Dinámico de Reintento de Fallidas -->
                                <Button x:Name="BtnRetryFailed" Click="BtnRetryFailed_Click" Visibility="Collapsed" HorizontalAlignment="Stretch" Padding="16,8">
                                    <StackPanel Orientation="Horizontal" Spacing="8" HorizontalAlignment="Center">
                                        <FontIcon Glyph="&#xE783;" Foreground="{ThemeResource SystemFillColorCautionBrush}" />
                                        <TextBlock x:Name="TxtRetryFailed" Text="Reintentar solo tablas fallidas (0)" FontWeight="SemiBold" Foreground="{ThemeResource SystemFillColorCautionBrush}" />
                                    </StackPanel>
                                </Button>
                            </StackPanel>
                        </StackPanel>
                    </Border>

                    <!-- Card de Progreso -->
                    <Border Background="{ThemeResource CardBackgroundFillColorDefaultBrush}" BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}" BorderThickness="1" CornerRadius="8" Padding="20">
                        <StackPanel Spacing="12">
                            <Grid>
                                <TextBlock x:Name="TxtCurrentTable" Text="Tabla: Ninguna en ejecución" FontWeight="SemiBold" Style="{StaticResource BodyTextBlockStyle}" HorizontalAlignment="Left" />
                                <TextBlock x:Name="TxtProgressPct" Text="0%" FontWeight="Bold" Style="{StaticResource BodyTextBlockStyle}" Foreground="{ThemeResource AccentTextFillColorPrimaryBrush}" HorizontalAlignment="Right" />
                            </Grid>

                            <ProgressBar x:Name="PbSync" Value="0" Maximum="100" Height="14" CornerRadius="7" HorizontalAlignment="Stretch" />

                            <TextBlock x:Name="TxtStatus" Text="Listo para iniciar" Style="{StaticResource CaptionTextBlockStyle}" Foreground="{ThemeResource TextFillColorSecondaryBrush}" />
                        </StackPanel>
                    </Border>
                </StackPanel>
            </ScrollViewer>

            <!-- Panel Derecho: Consola de Logs en Vivo -->
            <Border Grid.Column="1" Background="{ThemeResource LayerFillColorDefaultBrush}" BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}" BorderThickness="1" CornerRadius="8" Padding="16">
                <Grid RowSpacing="10">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="*" />
                    </Grid.RowDefinitions>

                    <!-- Barra superior de la consola con acciones -->
                    <Grid Grid.Row="0">
                        <TextBlock Text="Registro de Actividad (Trazabilidad)" Style="{StaticResource SubtitleTextBlockStyle}" VerticalAlignment="Center" HorizontalAlignment="Left" />

                        <StackPanel Orientation="Horizontal" Spacing="8" HorizontalAlignment="Right">
                            <Button x:Name="BtnCopyLogs" Click="BtnCopyLogs_Click" ToolTipService.ToolTip="Copiar todo el registro al portapapeles" Padding="10,5">
                                <StackPanel Orientation="Horizontal" Spacing="6">
                                    <FontIcon Glyph="&#xE8C8;" FontSize="13" />
                                    <TextBlock x:Name="TxtCopyBtnLabel" Text="Copiar Log" Style="{StaticResource CaptionTextBlockStyle}" />
                                </StackPanel>
                            </Button>
                            <Button x:Name="BtnClearLogs" Click="BtnClearLogs_Click" ToolTipService.ToolTip="Limpiar la consola de logs" Padding="10,5">
                                <StackPanel Orientation="Horizontal" Spacing="6">
                                    <FontIcon Glyph="&#xE74D;" FontSize="13" />
                                    <TextBlock Text="Limpiar" Style="{StaticResource CaptionTextBlockStyle}" />
                                </StackPanel>
                            </Button>
                        </StackPanel>
                    </Grid>

                    <!-- TextBox con scroll propio sin conflicto de ScrollViewer externo -->
                    <TextBox Grid.Row="1"
                             x:Name="TxtLogs" 
                             IsReadOnly="True" 
                             AcceptsReturn="True" 
                             TextWrapping="Wrap" 
                             ScrollViewer.VerticalScrollBarVisibility="Auto"
                             ScrollViewer.HorizontalScrollBarVisibility="Disabled"
                             ScrollViewer.VerticalScrollMode="Enabled"
                             FontFamily="Consolas, Cascadia Code, Courier New" 
                             FontSize="12" 
                             Background="Transparent" 
                             BorderThickness="0" 
                             HorizontalAlignment="Stretch" 
                             VerticalAlignment="Stretch" />
                </Grid>
            </Border>
        </Grid>
    </Grid>
</Page>
